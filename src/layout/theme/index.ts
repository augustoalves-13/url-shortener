import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#66D36E",
    },
    background: {
      default: "#121212",
      paper: "#121212"
    },
  },
});
